# 由于在加载远程配置之前，需要读取nacos配置中心的服务地址信息，
# 所以nacos服务地址等属性配置需要放在bootstrap.properties文件中
spring.cloud.nacos.config.server-addr=192.168.221.128:8848
# spring.cloud.nacos.config.prefix 表示Nacos配置中心上的Data Id的前缀
spring.cloud.nacos.config.prefix=example
spring.cloud.nacos.config.group=DEFAULT_GROUP